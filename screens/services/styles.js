import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    view: {
        alignItems: 'center',
        justifyContent: 'center',
        marginTop: '60%',
    },
    imageBackground: {
        height: '100%',
        width: '100%',
    },
});
